链表相关的题目

```
002
	两个数相加。
	关键是傀儡节点的使用。
019
	删除链表里倒数第N个数。
	这个的关键是快慢指针。
	先让一个指针走N步，
	如果发现已经走到了尾部（判断条件是cur->next为null）
		那么就返回head->next就好了。
	让cur一直走到最后一个元素上。
		同时让后面一个指针（名为pre）走相同的步数。
	pre这个跳过一个元素就好了。
021
	合并2个排序好的链表。
	傀儡节点。
023
	合并K个排序好的链表。
	分治法，然后调用合并2个链表的函数。
024
	两类交换链表里的节点。
	迭代来做就可以。
	还是要傀儡节点。
	也可以递归。
	
```

